# Frontend (Vite) — safe to expose, these are public Supabase keys.
VITE_SUPABASE_PROJECT_ID="your-project-ref"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"

# NOTE: M-Pesa / Daraja secrets are NOT set here. They are server-side
# only and live as Supabase Edge Function secrets. See README "M-Pesa setup".
